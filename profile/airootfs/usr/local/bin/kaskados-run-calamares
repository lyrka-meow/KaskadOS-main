#!/usr/bin/env bash
set -u

readonly calamares='/usr/bin/calamares'

if [[ ! -x "${calamares}" ]]; then
    printf 'KaskadOS: Calamares не найден.\n' >&2
    sleep 10
    exit 127
fi

exec "${calamares}"
